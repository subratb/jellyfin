apiVersion: v1
kind: Service
metadata:
  name: {{ include "sb-jellyfin.fullname" . }}
  labels:
    {{- include "sb-jellyfin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sb-jellyfin.selectorLabels" . | nindent 4 }}
